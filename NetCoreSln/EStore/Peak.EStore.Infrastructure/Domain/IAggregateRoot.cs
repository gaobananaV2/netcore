namespace Peak.EStore.Infrastructure.Domain
{
    public interface IAggregateRoot
    {
         
    }
}
//The aggregate root is a term used in domain-driven design (DDD) to define the entry point into a logical aggregation of related domain entities.
//The entity acting as the aggregate root will also have a corresponding repository to enable data persistence and retrieval. 